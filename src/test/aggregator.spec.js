import { createAggregator, createAggregatorFactory } from '../aggregator'

xdescribe('createAggregator', () => {
  it(`Given it exists
      Then it should be tested
  `, () => {})
})

xdescribe('createAggregatorFactory', () => {
  it(`Given it exists
      Then it should be tested
  `, () => {})
})
