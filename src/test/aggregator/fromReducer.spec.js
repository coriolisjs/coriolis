describe('fromReducer', () => {
  it('should test reducer function is converted to an aggregator function')
  it(
    'should test reducer function is not converted twice but resulting aggregator is re-used',
  )
})
